limits:
  total_cpu: 4          # cores across all containers
  total_memory_gb: 20   # GB across all containers
  total_disk_gb: 25     # GB across all containers
  per_agent_memory_gb: 5  # per Member / Diamond
  network: true         # all containers may reach the network
