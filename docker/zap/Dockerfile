# Shared ZAP MCP (OWASP ZAP). Placeholder for a dedicated service; this build
# integrates the ZAP MCP in-process (backend/mcp/shared.py).
FROM ghcr.io/zaproxy/zaproxy:stable
WORKDIR /srv
CMD ["sleep", "infinity"]
