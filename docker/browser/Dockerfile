# Shared Browser MCP (Playwright + Chromium). Placeholder for a dedicated
# service; this build integrates the Browser MCP in-process (backend/mcp/shared.py).
FROM mcr.microsoft.com/playwright/python:v1.44.0-jammy
WORKDIR /srv
CMD ["sleep", "infinity"]
